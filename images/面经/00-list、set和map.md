# List 、set和map的区别

### Java中的list和set的区别
list和set方法的区别有，list可以允许有重复的对象和插入多个null值，而set不允许；
list是容器是有序的，而set容器是无序的。

java中的集合共包含三大类，他们分别是Set（集）、List（列表）以及Map（映射）。
他们都处在java.util中并且都为接口。他们都有各自的实现了类，set的实现类主要有
Hashset和TreeSet，LIst的实现类主要有ArraryList。


